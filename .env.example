# ---- CupidList environment variables ----
# Copy to ".env.local" for local dev, or set these in your Vercel project.
# All of these are OPTIONAL: without them the app runs as a guest experience
# (Local Storage only). Set them to enable real "Sign in with LinkedIn".

# Required by Auth.js to sign session tokens. Generate one with:
#   openssl rand -base64 32
AUTH_SECRET=

# LinkedIn OAuth app credentials.
# Create an app at https://www.linkedin.com/developers/apps, add the
# "Sign In with LinkedIn using OpenID Connect" product, and set the redirect URL
# to:  https://<your-domain>/api/auth/callback/linkedin
AUTH_LINKEDIN_ID=
AUTH_LINKEDIN_SECRET=

# Flip to "true" once the above are set to show the "Sign in with LinkedIn"
# button in the UI. This one is public (NEXT_PUBLIC_) by design.
NEXT_PUBLIC_AUTH_ENABLED=false
